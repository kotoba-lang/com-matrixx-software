{:paths ["src" "test"]
 :tasks {test {:doc "Run the cljc contract + behavioral test under babashka"
               :extra-paths ["test"]
               :requires ([matrixx_software.main-test])
               :task (let [{:keys [fail error]} (clojure.test/run-tests 'matrixx_software.main-test)]
                       (System/exit (if (pos? (+ fail error)) 1 0)))}}}
